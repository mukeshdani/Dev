apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mukeshui-a-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: a.b18g55.online
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: mukeshui-a-service
            port:
              number: 80