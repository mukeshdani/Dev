apiVersion: v1
kind: Service
metadata:
  name: data-simulator-service
  namespace: digitalopsdev
spec:
  type: ClusterIP
  selector:
    app: data-simulator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
