apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: digitalopsdev
spec:
  type: ClusterIP
  selector:
    app: ingestion
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
